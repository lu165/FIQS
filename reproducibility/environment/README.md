# Environment Records

This directory contains both model configuration metadata and runtime environment records.

`base_model_config.json` describes the configuration information released with the pretrained model.

`lib_versions.txt` and `pip_freeze_20260815_1140.txt` record the actual software environment used for running experiments.

Therefore, version differences between model configuration metadata and runtime packages may exist and should not be interpreted as inconsistent experimental environments.
